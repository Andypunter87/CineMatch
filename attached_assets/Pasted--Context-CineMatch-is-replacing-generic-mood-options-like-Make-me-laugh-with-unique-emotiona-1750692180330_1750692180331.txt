🎬 Context:
CineMatch is replacing generic mood options (like “Make me laugh”) with unique, emotionally intuitive vibe labels generated by an LLM. These labels should reflect the user’s personality, preferences, and previous ratings — and they should feel clever, weird, or charming, like they came from a real film buff friend.

Unlike before, you do not need to match these to fixed enums. Just store them as-is for reuse in Firestore.

🧱 Goal:
Build a function called `generateMoodLabels(userData)` that uses the OpenAI API to generate 5 playful, personalised mood labels based on the user’s watch history and current context.

📥 Inputs:
The function should accept a `userData` object with the following shape:

```ts
{
  userRatedFilms: {
    title: string;
    genres: string[];
    rating: number;
  }[];
  viewingContext: {
    audience: "solo" | "friends" | "date" | "family";
    timeOfDay: string[]; // e.g. ["late", "weekend"]
  };
}
📤 Output:
Return a simple array of strings:

ts
Copy
Edit
[
  "Something slightly surreal but very warm",
  "A film that feels like a secret handshake",
  "Let me cry in a beautiful setting",
  "Make me feel like a clever detective",
  "Something nostalgic but not boring"
]
🧠 System Prompt:

txt
Copy
Edit
You are CineMate, a warm and clever film-obsessed friend who describes moods and vibes in delightfully quirky ways.

Given a user’s past preferences, generate 5 mood labels they might pick from when deciding what film to watch.

Each label should be:
- Short (max 12 words)
- Specific, emotional, and playful
- Avoid plain terms like “comedy” or “action” — focus on how the film makes them *feel*

Examples:
- “Let me feel like I’m on a mission”
- “Something odd, but wholesome”
- “Make me cry in a good way”
📦 Firestore:
Store the final set of labels in the user’s profile (or a separate userMoodLabels collection) along with a timestamp and session ID for tracking.

💡 Bonus:
If the LLM call fails, use a hardcoded set of fallback mood options stored in fallbackMoodLabels.ts.

yaml
Copy
Edit

---

Let me know if you'd like a starter `generateMoodLabels.ts` with:
- Axios or fetch integration with OpenAI
- Firestore write logic pre-wired  
- Fallback label logic in case of failure

Happy to draft that next!





